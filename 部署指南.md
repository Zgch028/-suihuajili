# 岁华纪丽网 · Cloudflare Pages 部署与域名绑定指南（单域名版）

> 适用对象：新手。全程鼠标点，能不敲命令就不敲。
> 目标：`npm run build` 的静态站自动部署到 Cloudflare Pages，主域名 `suihuajili.cn` 对外提供服务。
> 域名方案：**只注册 `suihuajili.cn` 一个**（比 `.com` 便宜一半多，且天然契合国内 ICP 备案）。不另购镜像域名，因此没有 301 跳转这一步。

---

## 0. 你已经具备的条件（先确认）

| 项目 | 状态 | 说明 |
|---|---|---|
| `astro.config.mjs` 的 `site` | ✅ 已设 `https://suihuajili.cn` | **不用改**，所有 SEO 产物已指向主域名 |
| `src/data/site.ts` 的 `url` | ✅ 已设 `https://suihuajili.cn` | `/agent.json` 等结构化数据指向主域名 |
| `wrangler.toml` | ✅ 已存在 | Pages 项目名 `suihuajili`、构建输出 `dist` |
| `.github/workflows/deploy.yml` | ✅ 已存在 | 推 `main` 自动构建并部署 |
| 独立 git 仓库 | ✅ 已 `git init` 并提交 | 与上层工作区仓库隔离，不会误推私文件 |
| 分支名 | ✅ 已是 `main` | 与工作流监听的分支一致 |

---

## 1. 在腾讯云注册 `suihuajili.cn`（注册商，只买域名 + 实名）

> 腾讯云只当"注册商"：买域名 + 改 NS。**网站托管仍在 Cloudflare Pages**（见后文）。

### 1.1 买域名
1. 浏览器打开 [cloud.tencent.com](https://cloud.tencent.com) → 微信扫码登录（无账号先注册）
2. 顶部搜索 **"域名注册"** → 进入「域名注册」控制台
3. 搜索框输入 `suihuajili` → 看 `.cn` 是否"可注册"（你已确认 `suihuajili.cn` 可注册 ✅；若显示"已注册"需换名）
4. 勾选 `suihuajili.cn` → **加入购物车** → 结算
5. 填**域名所有者**：选"个人"或"企业"，真实姓名 / 身份证（或营业执照）。⚠️ 国内注册必须**实名**，信息要真实
6. 支付（.cn 约 ¥20–40/年，按当时活动价；比 .com 便宜一半多）

> ⚠️ `.cn` 属 CNNIC 国别后缀，实名认证比 .com 更严格，且**实名通过前不能在 Cloudflare 正常解析**——务必等 1.2 状态变"已实名"再走第 4 步改 NS。

### 1.2 实名认证（必须，否则域名不能解析）
1. 控制台 → **域名注册** → 找到刚买的 `suihuajili.cn` → 点 **"实名认证"**
2. 上传身份证正反面（个人）/ 营业执照（企业）→ 提交
3. 等审核（几分钟～1 个工作日），状态变"已实名"即可解析

---

## 2. 推代码到 GitHub

### 方式 A：GitHub Desktop（推荐新手）
1. 浏览器打开 github.com 登录 → 右上角 **+** → **New repository**
2. Repository name 填 `suihuajili` → 选 **Public**
3. ⚠️ 三个都**别勾**：Add a README / Add .gitignore / Choose a license（本地都已有了）
4. 点 **Create repository** → 出来一个空仓库页面
5. 打开 GitHub Desktop（desktop.github.com），用同一账号登录
6. **File → Add Local Repository** → 路径选 `F:\安宇公司技术文件\龙山大佛工作台\岁华纪丽网_astro` → 确认
7. 左上角 **Publish repository** → 确认 Name 是 `suihuajili`、是 **Public** → **Publish**

### 方式 B：命令行（可选）
在该目录开终端执行（首次会弹浏览器登录）：
```bash
git remote add origin https://github.com/<你的用户名>/suihuajili.git
git push -u origin main
```

> ⚠️ 邮箱提醒：我第一次提交暂用了占位邮箱 `local@suihuajili.cn`。推送前在 GitHub Desktop 把 Git 邮箱改成你 GitHub 验证过的邮箱，否则提交不计入你的账号（不影响部署）。

---

## 3. 设两个 Secret（Cloudflare 凭证）

仓库页面 → **Settings**（右侧）→ **Secrets and variables** → **Actions** → **New repository secret**，加两个：

1. `CLOUDFLARE_API_TOKEN`
   - 去 dash.cloudflare.com → 右上角头像 → **My Profile** → **API Tokens** → **Create Token**
   - 模板选 **Edit Cloudflare Workers** 或自定义勾 **Account → Cloudflare Pages → Edit**
   - 生成后复制那串 token（**只显示一次**，存好）
2. `CLOUDFLARE_ACCOUNT_ID`
   - dash.cloudflare.com 右下角 "Account ID" 复制

两个都 **Add** 完。

---

## 4. 等自动部署，拿到 `*.pages.dev`

设好 Secret 后，仓库顶部 **Actions** 标签会出现一条 "Deploy to Cloudflare Pages" 正在跑。
变绿 ✅ 后：
- 去 Cloudflare 控制台 → **Workers & Pages** → `suihuajili`
- 记下分配的地址，形如 `https://suihuajili.pages.dev`（这就是临时访问地址）

---

## 5. 绑定主域名 `suihuajili.cn`（Cloudflare Custom Domain）

1. Cloudflare 控制台 → **Workers & Pages** → `suihuajili` → **Custom domains**（或 Settings → Domains）
2. 输入 `suihuajili.cn` → 按提示 **Activate**
3. Cloudflare 会要求验证你对域名的所有权——因为 DNS 还没交给它，页面会给出一条 **CNAME** 记录（形如 `suihuajili.cn → <项目名>.pages.dev`）让你去注册商（腾讯云）后台加
4. **去腾讯云改 NS**（关键一步，对接附录 1）：把 `suihuajili.cn` 的 Nameserver 改成 Cloudflare 给的两组 `*.ns.cloudflare.com`
5. 等 Cloudflare 显示 **"Active"**（NS 全球生效需几分钟到几小时），Custom domain 自动变绿
6. 访问 `https://suihuajili.cn` 应正常出站

> 改完 NS 后，域名 DNS 由 Cloudflare 管，腾讯云只保留"所有权 / 续费"。解析记录去 Cloudflare 加，不在腾讯云加。

---

## 6. 验证

- 浏览器开 `https://suihuajili.cn` → 正常出站 ✅
- 开浏览器「开发者工具 → Network」：状态码 **200**、`canonical` 链接指向 `https://suihuajili.cn` ✅
- 站内搜索（`/search`）、`/agent.json`、`/sitemap.xml` 均可用 ✅

---

## 7. 以后怎么更新文章

1. 在 `src/content/posts/` 里加/改 `.md` 文件
2. 提交并推到 GitHub（`git add -A && git commit -m "..." && git push`）
3. Actions 自动重新部署，几分钟后线上更新

---

## 附：国内备案后切又拍云（后续）

站里全部是静态文件（Pagefind 搜索 / JSON-LD / sitemap 无一例外），迁到又拍云**零改造**：
备案下来后，用 `upyun-cli` 把 `dist/` 推到又拍云存储空间，再把 DNS 指过去即可。Git 仍当源码源，不再走 Cloudflare。

---

## 附：如日后想加镜像域名（品牌保护，可选）

若担心 `suihuajili.com` / `suihuajili.wang` 被抢注，可另购并在 Cloudflare 给那个域名建 **301 规则** 跳到 `https://suihuajili.cn`（保留路径写法）：
- 匹配：Hostname equals `suihuajili.com`
- 目标（Dynamic）：`concat("https://suihuajili.cn", http.request.uri.path)`
- Status = 301，Preserve query string = On

本方案未购镜像域名，故无需此步。
