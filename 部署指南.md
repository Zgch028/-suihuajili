# 岁华纪丽网 · Cloudflare Pages 部署与双域名绑定指南

> 适用对象：新手。全程鼠标点，能不敲命令就不敲。
> 目标：`npm run build` 的静态站自动部署到 Cloudflare Pages，主域名 `suihuajili.com` 对外提供服务，`suihuajili.wang` 全站 301 跳转到 `.com`。

---

## 0. 你已经具备的条件（先确认）

| 项目 | 状态 | 说明 |
|---|---|---|
| `astro.config.mjs` 的 `site` | ✅ 已设 `https://suihuajili.com` | **不用改**，所有 SEO 产物已指向主域名 |
| `wrangler.toml` | ✅ 已存在 | Pages 项目名 `suihuajili`、构建输出 `dist` |
| `.github/workflows/deploy.yml` | ✅ 已存在 | 推 `main` 自动构建并部署 |
| 独立 git 仓库 | ✅ 已 `git init` 并提交 44 文件 | 与上层工作区仓库隔离，不会误推私文件 |
| 分支名 | ✅ 已是 `main` | 与工作流监听的分支一致 |

---

## 1. 推代码到 GitHub

### 方式 A：GitHub Desktop（推荐新手）
1. 浏览器打开 github.com 登录 → 右上角 **+** → **New repository**
2. Repository name 填 `suihuajili` → 选 **Public**
3. ⚠️ 三个都**别勾**：Add a README / Add .gitignore / Choose a license（本地都已有了）
4. 点 **Create repository** → 出来一个空仓库页面
5. 打开 GitHub Desktop（desktop.github.com），用同一账号登录
6. **File → Add Local Repository** → 路径选 `F:\安宇公司技术文件\龙山大佛工作台\岁华纪丽网_astro` → 确认
7. 左上角 **Publish repository** → 确认 Name 是 `suihuajili`、是 **Public** → **Publish**

### 方式 B：命令行（可选）
在该目录开终端执行（首次会弹浏览器登录）：
```bash
git remote add origin https://github.com/<你的用户名>/suihuajili.git
git push -u origin main
```

> ⚠️ 邮箱提醒：我第一次提交暂用了占位邮箱 `local@suihuajili.wang`。推送前在 GitHub Desktop 把 Git 邮箱改成你 GitHub 验证过的邮箱，否则提交不计入你的账号（不影响部署）。

---

## 2. 设两个 Secret（Cloudflare 凭证）

仓库页面 → **Settings**（右侧）→ **Secrets and variables** → **Actions** → **New repository secret**，加两个：

1. `CLOUDFLARE_API_TOKEN`
   - 去 dash.cloudflare.com → 右上角头像 → **My Profile** → **API Tokens** → **Create Token**
   - 模板选 **Edit Cloudflare Workers** 或自定义勾 **Account → Cloudflare Pages → Edit**
   - 生成后复制那串 token（**只显示一次**，存好）
2. `CLOUDFLARE_ACCOUNT_ID`
   - dash.cloudflare.com 右下角 "Account ID" 复制

两个都 **Add** 完。

---

## 3. 等自动部署，拿到 `*.pages.dev`

设好 Secret 后，仓库顶部 **Actions** 标签会出现一条 "Deploy to Cloudflare Pages" 正在跑。
变绿 ✅ 后：
- 去 Cloudflare 控制台 → **Workers & Pages** → `suihuajili`
- 记下分配的地址，形如 `https://suihuajili.pages.dev`（这就是临时访问地址）

---

## 4. 绑定主域名 `suihuajili.com`

1. Cloudflare 控制台 → **Workers & Pages** → `suihuajili` → **Custom domains**（或 Settings → Domains）
2. 输入 `suihuajili.com` → 按提示 **Activate**
3. 页面会给出一条 **CNAME** 记录让你加（若域名 DNS 已在 Cloudflare，会自动添加；若不在，去注册商后台加这条 CNAME）
4. 等几分钟变绿，访问 `https://suihuajili.com` 应正常出站

---

## 5. 把 `suihuajili.wang` 的 DNS 交给 Cloudflare

> 目的：让 `.wang` 也走 Cloudflare 边缘，才能用下面的 301 规则。

1. Cloudflare 控制台 → 左上角 **Add site** → 输入 `suihuajili.wang` → 选 **Free**
2. Cloudflare 会给两组 **NS** 记录（如 `xxx.ns.cloudflare.com` / `yyy.ns.cloudflare.com`）
3. 去 `.wang` 的注册商后台，把域名的 **Nameserver** 改成这两组
4. 等 Cloudflare 提示 "Active"（通常几分钟到几小时，取决于注册商生效）

⚠️ 注意：**不要**在 Pages 项目里把 `.wang` 也加为 Custom domain——它只负责跳转，不托管内容。

---

## 6. 设 301 跳转规则（`.wang` → `.com`，保留路径）

在 Cloudflare 控制台操作 **suihuajili.wang** 这个站点：

1. 左侧 **Rules** → **Redirect Rules** → **Create rule**（Single Redirect）
2. Rule name 填 `wang-to-com`
3. **匹配条件**：
   - Field = `Hostname`，Operator = `equals`，Value = `suihuajili.wang`
   - （可选再加一条：Hostname equals `www.suihuajili.wang`）
4. **Then**：选 **Dynamic** 重定向
   - Expression（目标 URL）填：
     ```
     concat("https://suihuajili.com", http.request.uri.path)
     ```
   - Status code = **301**
   - **Preserve query string = On**（保留 `?` 后的参数）
5. **Deploy**

效果：`suihuajili.wang/任意路径` → `https://suihuajili.com/任意路径`，且原路径与参数都保留。

> 小白备选（更简单但弱）：若暂不想折腾 Cloudflare 规则，可在 `.wang` 注册商后台用"域名转发 / URL Forwarding"整站转到 `https://suihuajili.com`。缺点：常不保留路径、HTTPS 支持参差。能上 Cloudflare 就优先用上面的规则法。

---

## 7. 验证

- 浏览器开 `https://suihuajili.com` → 正常出站 ✅
- 开 `http://suihuajili.wang/某文章` → 地址栏变成 `https://suihuajili.com/某文章` ✅
- 开浏览器「开发者工具 → Network」看跳转：状态码 **301**、`location` 指向 `.com` ✅

---

## 8. 以后怎么更新文章

1. 在 `src/content/posts/` 里加/改 `.md` 文件
2. 提交并推到 GitHub（`git add -A && git commit -m "..." && git push`）
3. Actions 自动重新部署，几分钟後线上更新

---

## 附：国内备案后切又拍云（后续）

站里全部是静态文件（Pagefind 搜索 / JSON-LD / sitemap 无一例外），迁到又拍云**零改造**：
备案下来后，用 `upyun-cli` 把 `dist/` 推到又拍云存储空间，再把 DNS 指过去即可。Git 仍当源码源，不再走 Cloudflare。

---

## 附录 A：在腾讯云（DNSPod）注册域名（新手）

> 腾讯云只当"注册商"：买域名 + 改 NS。网站托管仍在 Cloudflare Pages（见正文）。

### A.1 买域名
1. 浏览器打开 [cloud.tencent.com](https://cloud.tencent.com) → 微信扫码登录（无账号先注册）
2. 顶部搜索 **"域名注册"** → 进入「域名注册」控制台
3. 搜索框输入 `suihuajili` → 看 `.com` 和 `.wang` 是否"可注册"（显示"已注册"需换名）
4. 勾选 `suihuajili.com` 和 `suihuajili.wang` → **加入购物车** → 结算
5. 填**域名所有者**：选"个人"或"企业"，真实姓名 / 身份证（或营业执照）。⚠️ 国内注册必须**实名**，信息要真实
6. 支付（.com 约 ¥60–80/年，.wang 约 ¥20–40/年，按当时活动价）

### A.2 实名认证（必须，否则域名不能解析）
1. 控制台 → **域名注册** → 找到刚买的域名 → 点 **"实名认证"**
2. 上传身份证正反面（个人）/ 营业执照（企业）→ 提交
3. 等审核（几分钟～1 个工作日），状态变"已实名"即可解析

### A.3 把 DNS 交给 Cloudflare（对接正文第 5 步）
1. 先按正文第 5 步去 Cloudflare 把两个域名 **Add site**，拿到两组 NS（如 `xxx.ns.cloudflare.com`）
2. 回腾讯云控制台 → **域名注册** → 点域名 → **解析** / **修改 DNS 服务器**
3. 把默认 DNSPod 的 NS 改成 Cloudflare 给的两组 → 保存
4. 等 Cloudflare 显示 **"Active"**（NS 全球生效需几分钟到几小时）

> 改完 NS 后，域名 DNS 由 Cloudflare 管，腾讯云只保留"所有权 / 续费"。解析记录去 Cloudflare 加，不在腾讯云加。

### A.4 关于 `.wang` 的 301
按正文第 6 步，在 Cloudflare 给 `.wang` 建 301 跳转规则即可（推荐，保留路径）。不必在腾讯云设转发。
